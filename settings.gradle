plugins {
  id("com.gradleup.nmcp.settings").version("1.5.0")
}

rootProject.name = 'allotropy'


nmcpSettings {
  centralPortal {
	username = settings.providers.gradleProperty("centralUsername").get()
	password = settings.providers.gradleProperty("centralPassword").get()
  }
}